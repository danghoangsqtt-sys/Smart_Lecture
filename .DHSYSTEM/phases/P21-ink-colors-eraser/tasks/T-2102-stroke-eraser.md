# T-2102 — Per-stroke eraser and non-blocking converter check

## Result

Added a per-stroke eraser with direct stroke targeting and nearby-tap matching, preserving the existing undo/redo path. Made LibreOffice detection asynchronous in Teaching Hub preflight and rendered its in-progress state instead of blocking the page.
